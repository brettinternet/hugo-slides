const { highlight, reveal } = window.hugoPayload;
console.log(highlight.theme, reveal.theme);

// // require(`highlight.js/styles/${highlight.theme}.css`);
// import styles from `highlight.js/styles/a11y-dark.css`;
// console.log("styles: ", styles);

// const highlightLink = document.createElement("link");

// highlightLink.type = "text/css";
// highlightLink.href = styles;

// document.head.appendChild(highlightLink);
